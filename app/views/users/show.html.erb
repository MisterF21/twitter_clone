
<h2 id="show_username"><%= @user.username %></h2>
<%= link_to "Edit", edit_user_path, id: "update_user", remote: true, :class=> "btn btn-default btn-xs" %>
<%= link_to "Delete", user_path(@user), :data => { :confirm => "Are you sure?" },
                                        :method => "delete",
                                        :class => "btn btn-danger btn-xs" %>
<h3 id="show_email"><%= @user.email %></h3>

<%= link_to "Home Page", root_url, :class => 'btn btn-default btn-med' %>

