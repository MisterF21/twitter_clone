<h1>User Listing</h1>

<ul id="users">
  <% @users.each do |user| %>
    <li><%= link_to "#{user.username}", user_path(user) %></li>
  <% end %>
</ul>

<%= link_to "Signup", new_user_path, id: "new_user", remote: true, :class => "btn btn-default btn-med" %>
